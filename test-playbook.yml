---
- name: Intro to Ansible Playbooks
  hosts: all

  tasks:
  - name: Copy file hosts with permissions
    ansible.builtin.copy:
      src: /home/anton/testfile
      dest: /tmp/testfile
      mode: '0755'
